<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Tecno Motos" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <link href="https://fonts.googleapis.com/css2?family=NOMBRE_FUENTE:wght@300;400;500;600;700&display=swap" rel="stylesheet">
  </head>

  <body>
    <!-- Navigation -->
    <nav class="bg-white shadow-sm border-b border-gray-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between h-16">
          <div class="flex">
            <!-- Logo -->
            <div class="flex-shrink-0 flex items-center">
              <%= link_to root_path, class: "text-xl font-bold text-blue-600" do %>
                🏍️ Tecno Motos
              <% end %>
            </div>
            
            <!-- Navigation Links -->
            <div class="ml-6 flex space-x-8">
              <%= link_to "Clientes", clientes_path, 
                  class: "text-gray-900 hover:text-blue-600 inline-flex items-center px-1 pt-1 border-b-2 #{'border-blue-500' if current_page?(clientes_path)} text-sm font-medium transition duration-200" %>
              <%= link_to "Vehículos", vehiculos_path, 
                  class: "text-gray-900 hover:text-blue-600 inline-flex items-center px-1 pt-1 border-b-2 #{'border-blue-500' if current_page?(vehiculos_path)} text-sm font-medium transition duration-200" %>
              <%= link_to "Usuarios", users_path, 
                  class: "text-gray-900 hover:text-blue-600 inline-flex items-center px-1 pt-1 border-b-2 #{'border-blue-500' if current_page?(users_path)} text-sm font-medium transition duration-200" %>
            </div>
          </div>

          <!-- User Info & Actions -->
          <div class="flex items-center space-x-4">
            <% if user_signed_in? %>
              <!-- User Info -->
              <div class="flex items-center space-x-3">
                <div class="flex-shrink-0">
                  <div class="h-8 w-8 rounded-full bg-blue-600 flex items-center justify-center">
                    <span class="text-white font-medium text-xs">
                      <%= current_user.nombre.first %><%= current_user.apellido.first %>
                    </span>
                  </div>
                </div>
                <div class="text-sm">
                  <div class="font-medium text-gray-900"><%= current_user.nombre_completo %></div>
                  <div class="text-gray-500"><%= current_user.cargo_humanizado %></div>
                </div>
              </div>
              
              <!-- Logout Button -->
              <%= link_to "Cerrar Sesión", session_path, 
                  method: :delete,
                  class: "bg-red-500 hover:bg-red-600 text-white px-4 py-2 rounded-md text-sm font-medium transition duration-200" %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>

    <!-- Flash Messages -->
    <% if notice %>
      <div class="bg-green-50 border border-green-200 text-green-700 px-4 py-3" role="alert">
        <div class="max-w-7xl mx-auto">
          <%= notice %>
        </div>
      </div>
    <% end %>

    <% if alert %>
      <div class="bg-red-50 border border-red-200 text-red-700 px-4 py-3" role="alert">
        <div class="max-w-7xl mx-auto">
          <%= alert %>
        </div>
      </div>
    <% end %>

    <!-- Main Content -->
    <%= yield %>
  </body>
</html>
